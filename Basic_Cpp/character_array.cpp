#include<bits/stdc++.h>
using namespace std;

int32_t main(){
    // char ar[100] = "apple";
    // int i=0;

    // while (ar[i] != '\0'){
    //     cout << ar[i] << endl;
    //     i++;
    // }
    int n = 100;
    char ar[n+1];       // ** because to say that null char to STOP.
    cin >> ar;           // just we need to give the variable name.
    
    cout << ar << endl;
    cout << ar[3] << endl;

    return 0;
}